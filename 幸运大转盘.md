# 幸运大转盘 - 云端存储配置指南 (JSONBin.io 版)

目前项目已精简为使用 **JSONBin.io** 作为唯一云端数据存储方案。本指南将协助您完成数据库的初始化配置。

## 第一步：创建 JSON 数据库 (Bin)

1. 访问 [JsonBin.io](https://jsonbin.io/) 并注册/登录账号。
2. 点击 **"Create New Bin"**。
3. 在代码编辑器中粘贴以下初始数据结构（**极其重要：必须包含 `records` 数组**）：

```json
{
  "records": [],
  "last_updated": "2026-01-16T00:00:00.000Z"
}
```

4. 在右侧设置面板中，确保勾选 **"Private"** (私有模式) 以保护数据安全。
5. 点击 **"Create"** 保存。
6. 保存后，记录页面顶部的 **Bin ID** (例如: `testtesttesttesttest`)。

## 第二步：获取 API 密钥

1. 进入 JsonBin 的 **"API Keys"** 页面。
2. 找到 **"Master Key"** (通常以 `$2a$10$` 开头)。
3. 复制该密钥。

## 第三步：更新项目配置

打开项目根目录下的 `config.js` 文件，将获取到的信息填入：

```javascript
const JSONBIN_CONFIG = {
    apiKey: '您的 Master Key', 
    binId: '您的 Bin ID'
};
```

## 数据交互逻辑说明

*   **身份验证**：用户输入姓名后，系统会自动读取 Bin 中的 `records` 数组。如果姓名已存在，则直接跳转至结果页，防止重复抽奖。
*   **中奖保存**：转盘旋转开始后，系统会立即异步触发 `PUT` 请求，将新记录推送到 Bin 中，确保存储记录与动画表现 100% 一致。
*   **奖品池管理**：系统会实时计算 `records` 中的奖品消耗情况，自动扣减剩余奖品数。

## 故障排查

*   **读取失败**：请检查 `apiKey` 是否正确，或 Bin 是否被设置为 Private。
*   **保存失败**：请确保 Bin 的初始 JSON 结构包含 `"records": []` 字段，否则代码无法识别数据格式。
*   **跨域问题**：JsonBin 默认支持 Web 端调用，如遇到网络波动，请检查网络连接或尝试刷新页面。
